apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kiali-anyflow-net
  namespace: cluster
spec:
  secretName: kiali-anyflow-net-tls
  issuerRef:
    name: letsencrypt-production-http01
    kind: ClusterIssuer
  dnsNames:
    - kiali.anyflow.net
