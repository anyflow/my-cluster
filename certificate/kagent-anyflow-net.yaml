apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kagent-anyflow-net
  namespace: cluster
spec:
  secretName: kagent-anyflow-net-tls
  issuerRef:
    name: letsencrypt-production-http01
    kind: ClusterIssuer
  dnsNames:
    - kagent.anyflow.net
